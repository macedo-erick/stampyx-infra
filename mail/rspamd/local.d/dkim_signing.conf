# Keys are written per domain by the API when it generates them. selector matches the
# dkim_selector column, which defaults to "stampyx".
selector = "stampyx";
path = "/var/lib/rspamd/dkim/$domain.$selector.key";
allow_username_mismatch = true;
use_domain = "header";
sign_authenticated = true;
sign_local = true;
